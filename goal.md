# 桌面端完整功能验收目标

验证当前 `codex/desktop-one-click-ready` 工作树可以交付为 macOS 本机桌面应用，并形成以下闭环：桌面端自启动本机后端、一键环境准备、Google Play 模拟器准入、网络与 Frida 检查、Jenkins/本地 APK 安装、应用启动、实时抓包、请求与响应查看、停止后的资源清理，以及可下载发行包完整性。

## 当前增量目标：桌面端交互与精准文案 V1.1

将原生 macOS 桌面端收敛为 `抓包 / 安装应用 / 接口 / 日志` 四个主导航，并把默认入口改为“抓包”。普通用户只需点击“开始抓包”，工具自动启动模拟器、等待 Android 和屏幕解锁、识别前台应用、准备抓包环境并开始记录接口。

所有普通界面状态必须说明“正在处理什么、当前结果是什么、用户下一步做什么”。Frida、AVD、HTTP 状态冲突、Session、端口及后端原始 JSON 仅允许出现在“运行检查”的技术详情中。业务文案统一集中到 `AppCopy`，抓包页面由 `CaptureWorkflowState` 驱动，错误由 `UserFacingIssue` 映射为可恢复的用户提示。

接口页继续实时更新，支持“清空列表”但不停止抓包；安装应用页保留 Jenkins 与本地 APK；日志页使用面向用户的中文操作文案。成功提示 3 秒自动消失，失败提示 4 秒自动消失，阻塞状态保留在页面中。

## 当前增量目标：Android 运行日志控制台

将桌面端左侧现有“日志”入口从占位页升级为实时 Android Logcat 控制台。用户选择设备和应用后，默认自动查看目标应用进程日志，并可切换系统日志与崩溃日志；页面支持暂停、继续、清空当前视图、关键字搜索、日志级别过滤和自动滚动。

日志功能必须按设备隔离，不依赖 Frida，不修改 Android 全局代理，不执行 `adb logcat -c`，不持久化可能包含账号或 Token 的原始日志。桌面端停止读取或设备释放后，后台 Logcat 进程必须自动退出。

### 真机日志增量

日志页同时支持 Android 模拟器和通过 USB 或无线调试连接的 Android 真机。真机只需开启开发者选项、启用调试并授权当前 Mac；读取 Logcat 不要求 Root，不启动 Frida，不改写设备网络或代理。桌面端进入日志页时自动发现已授权设备，并清晰区分模拟器、USB 真机、无线真机及“等待授权”状态。

设备发现不得覆盖、改名或停用已有模拟器池配置。真机断开、授权撤销、切换设备或离开日志页时，界面必须停止无效轮询，并立即结束本工具为对应设备启动的 Logcat 子进程；普通界面只显示原因与恢复动作，ADB 原始信息保留在技术详情。

## 当前增量目标：以前台应用为准的自动抓包流程

解除“抓包目标必须先从应用库或 Jenkins 列表选择”的限制。用户可以通过桌面端选择本地 APK、从 Jenkins 安装 APK，或使用外部方式把 App 安装到当前设备；只要用户在设备中打开待测 App，桌面端就自动识别前台包名和 Activity、登记或复用应用记录，并检查该 App 的抓包准入状态。

桌面端必须明确区分“已识别前台 App”“抓包链路可启动”和“已捕获目标 App 流量”。只有实际捕获到当前包对应 Session 的接口后才能标记“可抓包”；没有产生网络请求时显示“等待操作”，不能误判失败或成功。Jenkins 只作为 APK 来源，不再作为抓包目标选择器。

该增量不修改 `flutter-socks + Frida + mitmproxy/exporter` 底层协议，不自动切换正在运行的其他 App Session，不执行卸载、清数据或 wipe。

## 当前增量目标：退出桌面端时结束活动抓包

修复桌面端完全退出后抓包子进程仍在后台运行、再次打开时误显示“正在抓包”并阻止安装的问题。桌面端退出时必须先请求其拥有的本机后端停止全部活动抓包，再结束后端；停止过程保留抓包结果和模拟器状态，只清理本项目抓包进程、项目端口与 Android 抓包代理。

清理请求必须有明确的短超时。即使清理接口失败或无响应，桌面端也必须继续退出，不能卡住；未由桌面端拥有或接管的外部后端不得收到退出清理请求，也不得被终止。

## 当前增量目标：日志清空与高频日志降噪

修复清空日志后旧分页响应可能重新写回界面，以及日志重新连接时回放 Android 历史缓冲区的问题。新日志连接只读取连接时附近及之后的内容；清空后只展示清空完成后产生的新日志，不执行会影响其他调试工具的全局 `adb logcat -c`。

桌面端将同一条 Android 多行输出合并为一条逻辑日志，并把内容完全相同的连续日志折叠为一条计数记录。搜索、级别过滤、复制和页面计数均基于逻辑日志，底层原始条目仍保留在本机有界内存中，便于保持增量游标和完整内容。

## 当前增量目标：长日志紧凑浏览

修复超长 Base64、JSON 和异常堆栈完整铺开后挤占日志表格的问题。表格默认只显示可辨识的紧凑预览，用户可按单条日志展开或收起完整内容；纯文本模式、搜索和“复制全部”继续使用完整日志，不截断、不丢失采集结果。

## 当前增量目标：超大接口响应稳定展示

修复接口详情打开超大 JSON 或文本响应时，SwiftUI 在主线程一次性排版完整内容并导致桌面端长时间无响应的问题。接口详情只内联加载有界预览，完整请求和响应文件继续保存在本机抓包目录，并允许用户从桌面端定位原始文件。

详情文本使用适合长内容的原生只读文本视图，不再让 SwiftUI `Text` 计算整份响应的完整布局。生成 cURL 时不得重复读取无关的 Response；大响应保护不得影响接口列表轮询、抓包文件留存或小响应的完整展示。

## 当前增量目标：发行包内置安装说明

桌面 ZIP 根目录必须同时包含 `抓包工具.app`、面向普通用户的 `安装说明.md`，以及供通用 ZIP 工具读取的同内容 `INSTALL.md`。说明需要明确 macOS 与硬件要求、Android SDK/Google Play 模拟器和网络前置条件、安装与首次放行步骤、首次环境准备、基础抓包流程以及常见问题。

打包流程必须自动校验说明文件存在，开发预览包与正式分发包使用同一份内容。说明不得包含账号、密码、Token、本机绝对路径或关闭 Gatekeeper 等不安全操作。

## 边界

- 仅验收 macOS + Android。
- 不验证 Windows、Linux、iOS 或服务器多人模式。
- 不执行 `wipe-data`、`pm clear`，不删除 Google/App 登录态。
- 不停止或修改不属于本项目的模拟器、端口或进程。
- 桌面端退出不关闭模拟器，不卸载应用，不清除登录态或历史抓包文件。
- Google 密码和登录动作由用户自行完成；验收只检查准入状态。
- 不修改 `flutter-socks + Frida + mitmproxy/exporter` 底层抓包协议。
- 不在普通页面直接显示后端原始错误、实现术语或设备技术标识。

## 验收命令与证据

- `python -m unittest discover tests`
- `npm --prefix web run build`
- `macos-native/scripts/build-app.sh`
- `release/package.sh`
- `python -m unittest -v tests.test_logcat_service tests.test_console_api`
- `swift test --package-path macos-native`
- `rg -n "Session|active session|执行中|未就绪|HTTP 409" macos-native/Sources/AICaptureNativeApp --glob '*.swift'`
- 真实启动 `AI抓包工具.app`，检查 `127.0.0.1:7001`、Environment Doctor、Google Play 镜像、设备 Doctor、Frida、Jenkins、抓包 session、flow detail/cURL。
- 真实启动 `抓包工具.app`，逐页检查四个主导航、自动抓包状态、安装错误、接口空/清空/实时恢复和日志断线提示。
- 在真实模拟器中启动一个测试 App，确认“日志”页能显示应用日志、系统日志和崩溃缓冲区，切换设备或离开页面后无孤儿 `adb logcat` 进程。
- 使用模拟 ADB 输出验证 USB/无线真机发现、未授权提示、稳定设备 ID 和模拟器配置保留；有真实真机连接时追加应用/系统/崩溃三类日志实机验收。
- 检查发行包包含桌面 App 与内嵌后端，且不包含开发 runtime、数据库、账号和抓包历史。
