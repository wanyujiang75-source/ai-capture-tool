[package]
name = "ai-capture-desktop"
version = "1.0.0"
description = "AI抓包工具 macOS desktop shell"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
ureq = "2"
